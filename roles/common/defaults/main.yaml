vm_install: true

common_packages:
  - ca-certificates
  - gnupg
  - vim
  - git
  - curl
  - netcat
  - dnsutils
  - stow
  - tree
  - htop
  - jq

common_vm_packages:
  - qemu-guest-agent
  - haveged

debian_packages:
  - lsb-release
  - nfs-common

os_timezone: "America/Chicago"

ssl_directory: "/etc/ssl"